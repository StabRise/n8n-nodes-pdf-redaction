# Changelog

## 0.1.0

- Initial release: **PDF Redaction** node with `Anonymize`, `Anonymize with Custom Prompt`, and `Detect PII` operations backed by the [PDF Redaction API](https://api.pdf-redaction.com).
- **PDF Redaction API** credential (API key, header `X-API-Key`).
